{
    "name": "@olula/ctx",
    "version": "0.0.1",
    "type": "module",
    "types": "./**/declarations.d.ts",
    "exports": {
        "./*": "./src/*"
    },
    "scripts": {
        "lint": "eslint .",
        "type-check": "tsc -p ./tsconfig.json --noEmit",
        "ci": "pnpm lint && pnpm type-check && pnpm test run",
        "test": "vitest"
    },
    "dependencies": {
        "@olula/componentes": "workspace:*",
        "@olula/lib": "workspace:*"
    }
}